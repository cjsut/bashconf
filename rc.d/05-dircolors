# -*- shell-script -*-

# enable color support of ls and also add handy aliases
if [ "$TERM" != "dumb" ]; then
    if -n "$BASHCONF_ON_OSX"; then
	export CLICOLOR=yes
	export LSCOLORS="exfxcxdxbxegedabagacad"
    else
	eval "`dircolors -b`"
    fi
fi
